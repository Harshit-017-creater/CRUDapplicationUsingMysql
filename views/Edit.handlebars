<h3>{{viewTitle}}</h3>
<form action="/finalUpdate" method="post">
<input type="hidden" name = "_id"value="{{employee.id}}"/>
    <div class="form-group">
        <label>Full Name</label>
         <input type="text" class="form-control" name="name" placeholder="Enter the Full Name" value="{{list.name}}">
    </div>
    <div class="form-group">
        <label>Email</label>
        <input type="email" class="form-control" name="email" placeholder="Enter the E-mail" value="{{list.email}}">
    </div>
        <div class="form-row">
            <div class="form-group col-md-6">
        <label>Mobile</label>
        <input type="text" class="form-control" name="pno" placeholder="Enter the Mobile" value="{{list.pno}}">
        </div>
        <div class="form-group col-md-6">
        <label>city</label>
        <input type="text" class="form-control" name="password" placeholder="Enter the City" value="{{list.password}}">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-info"><i class="fa fa-database"></i>Submit</button>
        </div>
    </div>

</form>